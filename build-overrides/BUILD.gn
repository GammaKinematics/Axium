# Axium Engine - Minimal root BUILD.gn
# Testing with just //content to isolate dependencies

import("//build/config/features.gni")

group("gn_all") {
  deps = [
    "//content",
    "//content/public/app",
  ]
}

group("default") {
  deps = [ ":gn_all" ]
}

group("all") {
  deps = [ ":gn_all" ]
}
